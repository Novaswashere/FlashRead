export const DEFAULT_SPEED_WPM = 350;
export const DEFAULT_THEME = "light" as const;
export const DEFAULT_FONT = "Inter" as const;
export const DEFAULT_FONT_SIZE = 48;
export const SUPPORTED_FILE_TYPES = [".epub", ".pdf", ".txt"] as const;
